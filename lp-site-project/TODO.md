# Todo List

## 1. Asset Baseline
- [x] Copy generated UI mock to `public/assets/ui-reference.png`.
- [x] Copy generated hero image to `public/assets/hero-urban-rose-gold.png`.
- [x] Generate and copy lower CTA photo to `public/assets/cta-rose-tray.png`.
- [x] Generate rose-gold icon sheet and chroma-key remove it to `public/assets/icons-transparent.png`.

## 2. Worker A: Project Scaffold
- [x] Create React + Tailwind project files without touching visual component implementation.
- [x] Own files: `package.json`, `index.html`, `vite.config.js`, `tailwind.config.js`, `postcss.config.js`, `src/main.jsx`.
- [x] Ensure the scaffold points to `src/App.jsx` and `src/index.css`.

## 3. Worker B: React UI Structure
- [x] Implement the LP component structure and Japanese copy.
- [x] Own files: `src/App.jsx`, `src/components.jsx`.
- [x] Match the reference layout: hero, service/features, process, pricing, FAQ, contact strip, bottom benefits.

## 4. Worker C: Tailwind Styling and Animations
- [x] Implement the Tailwind/CSS layer and animation behavior.
- [x] Own files: `src/index.css`, `src/animations.js`.
- [x] Match reference colors, spacing, borders, typography scale, responsive behavior, fade-in animation, icon-sheet crops.

## 5. Orchestrator QA and PDCA
- [x] Review worker outputs and integrate conflicts.
- [x] Run available validation commands.
- [x] Use Browser Use for visual checks if local URL access is allowed.
- [x] If Browser Use is blocked, perform alternate static/headless checks and document the limitation.
- [x] Iterate fixes until the implementation meets the visual and functional baseline.
